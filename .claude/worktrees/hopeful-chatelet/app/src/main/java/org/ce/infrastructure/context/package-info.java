/**
 * Infrastructure calculation context objects.
 *
 * <p>These classes carry assembled, validated runtime input state for
 * CVM and MCS execution. They are intentionally placed in infrastructure,
 * not domain, because they bind orchestration data from registry/cache/ECI
 * loading concerns.</p>
 */
package org.ce.infrastructure.context;

